# Simple C trie-tree
A simple and convenient library for trie-tree.

## Use
Just include these single-header file `sctrie.h`.
And define their `IMPL` macros to get the definitions for compiling.
- `#include "sctrie.h"`: `SCTRIE_IMPL`
